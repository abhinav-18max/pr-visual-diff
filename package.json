{
  "name": "pr-visual-diff-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/*/src/*.test.js",
    "publish-packages": "node scripts/publish.js"
  }
}
