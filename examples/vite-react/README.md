# Vite React example

Use this folder for a minimal React + Vite demo app that exercises:

- `/`
- `/dashboard`

Suggested scripts:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173"
  }
}
```
